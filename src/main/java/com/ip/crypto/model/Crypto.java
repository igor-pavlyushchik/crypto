package com.ip.crypto.model;

/**
 * Before introducing Spring security, let's limit within enum the possible options for crypto symbols.
 */
public enum Crypto {
    BTC,
    ETH,
    LTC,
    XRP,
    DOGE
}
